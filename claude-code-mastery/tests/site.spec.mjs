import {test,expect} from '@playwright/test';
test.beforeEach(async({page})=>{await page.goto('/');});
test('loads without critical console errors and has complete PDF curriculum',async({page})=>{const errors=[];page.on('console',m=>{if(m.type()==='error')errors.push(m.text())});await expect(page).toHaveTitle(/Claude Code Mastery/);await expect(page.getByRole('heading',{name:/Claude Code Mastery/})).toBeVisible();await expect(page.locator('.lesson')).toHaveCount(27);await expect(page.locator('.topic-dossier')).toHaveCount(95);await expect(page.locator('.source-card')).toHaveCount(10);await expect(page.locator('.coverage-table tbody tr')).toHaveCount(95);expect(errors).toEqual([]);});
test('synthesized lessons are primary and raw pages are only a lazy evidence appendix',async({page})=>{const order=await page.evaluate(()=>['deep-topics','quick-start','pdf-reader'].map(id=>[id,document.getElementById(id).getBoundingClientRect().top]));expect(order[0][1]).toBeLessThan(order[1][1]);expect(order[1][1]).toBeLessThan(order[2][1]);await expect(page.locator('#deep-topics .eyebrow')).toContainText('Primary learning path');await expect(page.locator('#pdf-reader .eyebrow')).toContainText('Evidence appendix');await expect(page.locator('#courseNav a')).toHaveCount(95);expect(await page.locator('#courseNav a').evaluateAll(xs=>xs.every(x=>x.getAttribute('href').startsWith('#topic-')))).toBe(true);await expect(page.locator('.pdf-volume[open]')).toHaveCount(0);await expect(page.locator('.pdf-page[open]')).toHaveCount(0);await expect(page.locator('.page-text')).toHaveCount(0);});
test('primary curriculum integrates all lessons once without optional segregation or fake levels',async({page})=>{await expect(page.locator('#topicHandbook>.topic-phase')).toHaveCount(5);const ids=await page.locator('#topicHandbook .topic-dossier').evaluateAll(xs=>xs.map(x=>x.id));expect(ids).toHaveLength(95);expect(new Set(ids).size).toBe(95);await expect(page.locator('#topicHandbook')).not.toContainText(/Level \d+|Optional Labs|Optional lesson/i);await expect(page.locator('#topicHandbook>.topic-phase[open]')).toHaveCount(1);await expect(page.locator('#topicHandbook .topic-level[open]')).toHaveCount(1);});
test('deep topic dossiers contain teaching and page-level evidence',async({page})=>{const first=page.locator('.topic-dossier').first();await first.locator(':scope > summary').click();await expect(first).toHaveAttribute('open','');await expect(first.locator('.topic-dossier-body')).toContainText('Why this matters');await expect(first.locator('.steps li')).toHaveCount(6);await expect(first.locator('.failure-list li')).toHaveCount(3);await expect(first.locator('.worked-example')).toBeVisible();await expect(first.locator('.page-citation')).not.toHaveCount(0);await first.locator('.page-citation').first().click();await expect(page).toHaveURL(/#reader-pdf-.+-page-\d+/);});
test('all lessons expose source-specific mechanisms and traceable pages',async({page})=>{const ids=await page.locator('.topic-dossier').evaluateAll(xs=>xs.map(x=>x.id));for(const id of ids){const lesson=page.locator(`#${id}`);await lesson.evaluate(el=>{el.open=true;el.dispatchEvent(new Event('toggle',{bubbles:true}))});await expect(lesson.locator('.source-mechanisms')).toHaveCount(1);await expect(lesson.locator('.source-mechanisms .page-citation').first()).toHaveAttribute('href',/#reader-pdf-.+-page-\d+/);}});
test('professional practicum contains projects, labs, rubrics, and capstone gates',async({page})=>{await expect(page.locator('.practice-project')).toHaveCount(8);await expect(page.locator('.guided-lab')).toHaveCount(25);await expect(page.locator('.rubric-table')).toHaveCount(8);await expect(page.locator('.independent-assignment')).toHaveCount(8);await expect(page.locator('.model-solution')).toHaveCount(8);await expect(page.locator('.capstone-track li')).toHaveCount(8);await expect(page.locator('.practice-project .linked-topics a')).toHaveCount(95);});
test('formerly segregated lessons are integrated and show evidence boundaries',async({page})=>{const ids=['context-projects-fit','source-cowork-code','source-model-routing','tool-html-infographics','knowledge-retrieval-cadence','source-creative-opinions','source-system-prompt-slimming','source-enterprise-seats','extensions-dx-setup-accounts'];for(const id of ids){const lesson=page.locator(`#topic-${id}`);await lesson.evaluate(el=>{el.open=true;el.dispatchEvent(new Event('toggle',{bubbles:true}))});await expect(lesson.locator('.evidence-boundary')).toHaveCount(1);await expect(lesson.locator('.evidence-boundary')).toContainText('Verify now');}});
test('navigation and disclosures open the phase-level-lesson hierarchy',async({page})=>{await page.locator('#courseNav .nav-level').first().evaluate(el=>el.open=true);await page.locator('#courseNav a').first().click();await expect(page).toHaveURL(/#topic-found-goal-vs-task/);const topic=page.locator('#topic-found-goal-vs-task');await expect(topic).toHaveAttribute('open','');await expect(topic.locator('xpath=ancestor::details[contains(@class,"topic-level")]')).toHaveAttribute('open','');await expect(topic.locator('xpath=ancestor::details[contains(@class,"topic-phase")]')).toHaveAttribute('open','');await expect(page.locator('#crumbs')).toContainText('Curriculum');await page.getByRole('button',{name:'Collapse all'}).click();await expect(topic).not.toHaveAttribute('open','');await page.getByRole('button',{name:'Expand all'}).click();await expect(topic).toHaveAttribute('open','');});
test('search and combined filters work',async({page})=>{await page.locator('#searchInput').fill('worktree');await expect(page.locator('.searchable:not(.hidden-by-filter)')).not.toHaveCount(0);await page.locator('#typeFilter').selectOption('lesson');await expect(page.locator('.lesson:not(.hidden-by-filter)')).not.toHaveCount(0);await page.locator('#difficultyFilter').selectOption('1');await expect(page.locator('.lesson:not(.hidden-by-filter)')).toHaveCount(0);await expect(page.locator('#emptyState')).toBeVisible();await page.getByRole('button',{name:'Clear filters'}).click();await expect(page.locator('#emptyState')).toBeHidden();});
test('theme, 95-lesson progress, bookmarks, and checklist persist',async({page})=>{await page.locator('#themeToggle').click();const topic=page.locator('.topic-dossier').first();await topic.locator(':scope > summary').click();await topic.locator('[data-topic-complete]').click();await page.locator('[data-bookmark="lesson-agent-loop"]').click();await topic.locator('[data-check]').first().check();await expect(page.locator('#progressLabel')).toContainText('1% complete');const phase=topic.locator('xpath=ancestor::details[contains(@class,"topic-phase")]');await expect(phase.locator('[data-topic-phase-progress]')).toContainText('1/');await page.reload();await expect(page.locator('html')).toHaveAttribute('data-theme','dark');await topic.locator(':scope > summary').click();await expect(topic.locator('[data-topic-complete]')).toContainText('Lesson completed');await expect(page.locator('[data-bookmark="lesson-agent-loop"]')).toHaveClass(/active/);await expect(topic.locator('[data-check]').first()).toBeChecked();});
test('copy, citations, quiz, tabs, and decision interactions work',async({page,context})=>{await context.grantPermissions(['clipboard-read','clipboard-write']);const copy=page.locator('[data-copy]').first();await copy.click();await expect.poll(()=>page.evaluate(()=>navigator.clipboard.readText())).not.toBe('');await page.locator('button[data-source]:visible').first().click();await expect(page.locator('#sourceDialog')).toBeVisible();await page.locator('.dialog-close').click();const lesson=page.locator('#lesson-agent-loop');await lesson.locator('input[type=radio]').nth(1).check();await lesson.locator('[data-quiz]').click();await expect(lesson.locator('.quiz-result')).toContainText('Correct');await lesson.locator('[role=tab]').nth(1).click();await expect(lesson.locator('[role=tabpanel]').nth(1)).toBeVisible();await page.locator('[data-decision]').first().click();await expect(page.locator('.decision-result').first()).not.toContainText('Choose a direction');});
test('mobile drawer and responsive overflow',async({page})=>{await page.setViewportSize({width:390,height:844});await page.locator('#menuToggle').click();await expect(page.locator('#sidebar')).toHaveClass(/open/);await page.locator('#menuClose').click();await expect(page.locator('#sidebar')).not.toHaveClass(/open/);const overflow=await page.evaluate(()=>document.documentElement.scrollWidth>document.documentElement.clientWidth);expect(overflow).toBe(false);});
test('keyboard focus and internal links are valid',async({page})=>{await page.keyboard.press('Tab');await expect(page.locator('.skip-link')).toBeFocused();const broken=await page.evaluate(()=>[...document.querySelectorAll('a[href^="#"]')].map(a=>a.getAttribute('href').slice(1)).filter(id=>id&&!document.getElementById(id)));expect(broken).toEqual([]);});
test('all local images and scripts load',async({page})=>{const failed=[];page.on('requestfailed',r=>failed.push(r.url()));await page.reload();expect(failed).toEqual([]);await expect(page.locator('script[src]')).toHaveCount(1);});
test('all mapped lesson visuals load',async({page})=>{const images=page.locator('.lesson-visual img');await expect(images).toHaveCount(19);await images.evaluateAll(xs=>xs.forEach(x=>x.loading='eager'));await expect.poll(()=>images.evaluateAll(xs=>xs.filter(x=>x.complete&&x.naturalWidth>0).length)).toBe(19);const broken=await images.evaluateAll(xs=>xs.filter(x=>!x.complete||x.naturalWidth===0).map(x=>x.src));expect(broken).toEqual([]);});
test('new source diagrams render with visible attribution',async({page})=>{const ids=['found-agent-loop','found-reflection-rule','context-engineering','parallel-multi-agent-roles','ralph-feedback','ralph-levels','ralph-patterns','tool-html-infographics','source-cowork-code','knowledge-obsidian-import','knowledge-provenance','instructions-progressive-disclosure'];for(const id of ids){const lesson=page.locator(`#topic-${id}`);await lesson.evaluate(el=>{let p=el;while(p){if(p.tagName==='DETAILS')p.open=true;p=p.parentElement}el.dispatchEvent(new Event('toggle',{bubbles:true}))});const image=lesson.locator('.lesson-visual img');await expect(image).toHaveCount(1);await image.evaluate(el=>el.loading='eager');await expect.poll(()=>image.evaluate(el=>el.complete&&el.naturalWidth>0)).toBe(true);await expect(lesson.locator('.lesson-visual figcaption')).toContainText('Source:');}});
test('complete PDF evidence appendix is available only on explicit inspection',async({page})=>{await expect(page.locator('.pdf-volume')).toHaveCount(10);await expect(page.locator('.pdf-page')).toHaveCount(352);await expect(page.locator('.concept-index a')).toHaveCount(268);await expect(page.locator('.page-text')).toHaveCount(0);await page.goto('/#reader-pdf-ruben-27-page-4');await expect(page.locator('#reader-pdf-ruben-27')).toHaveAttribute('open','');await expect(page.locator('#reader-pdf-ruben-27-page-4')).toHaveAttribute('open','');await expect(page.locator('#reader-pdf-ruben-27-page-4 .page-text')).toContainText('Stop it with your Claude');await expect(page.locator('.page-text')).toHaveCount(1);});
for(const v of [{name:'desktop-light',width:1440,height:1000},{name:'tablet-dark',width:820,height:1100},{name:'mobile-light',width:390,height:844}])test(`screenshot ${v.name}`,async({page})=>{await page.setViewportSize({width:v.width,height:v.height});if(v.name.includes('dark'))await page.locator('#themeToggle').click();await page.screenshot({path:`screenshots/${v.name}.png`,fullPage:false});});
test('screenshots primary curriculum, expanded teaching, search, and sources',async({page})=>{await page.locator('#deep-topics').scrollIntoViewIfNeeded();await page.screenshot({path:'screenshots/primary-curriculum.png',fullPage:false});await page.goto('/#topic-found-goal-vs-task');const topic=page.locator('#topic-found-goal-vs-task');await expect(topic).toHaveAttribute('open','');await expect(topic.locator('.topic-dossier-body')).toContainText('Worked application');await topic.scrollIntoViewIfNeeded();await page.screenshot({path:'screenshots/deep-topic-expanded.png',fullPage:false});await page.locator('#searchInput').fill('context');await page.screenshot({path:'screenshots/search-results.png',fullPage:false});await page.locator('#sources').scrollIntoViewIfNeeded();await page.screenshot({path:'screenshots/source-explorer.png',fullPage:false});});
