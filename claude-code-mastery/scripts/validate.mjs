import fs from 'node:fs';import path from 'node:path';import crypto from 'node:crypto';
const root=path.resolve(import.meta.dirname,'..');const read=n=>JSON.parse(fs.readFileSync(path.join(root,'data',n),'utf8'));
const sources=read('source-manifest.json'),insights=read('insights.json'),curriculum=read('curriculum.json'),curriculumMap=read('curriculum-map.json'),ref=read('reference.json'),topics=read('topic-coverage.json'),topicDetailsFile=read('topic-details.json'),topicDetails=Array.isArray(topicDetailsFile)?topicDetailsFile:topicDetailsFile.topics,images=read('image-manifest.json'),pdfReader=read('pdf-reader.json');
const pdfManifest=read('pdf-source-manifest.json');
const assert=(v,m)=>{if(!v)throw new Error(m)};assert(sources.length===10,'All 10 sources required');assert(new Set(sources.map(s=>s.id)).size===10,'Duplicate source IDs');
for(const s of sources){for(const k of ['id','url','title','retrievedAt','extractionMethod','status','coverageNotes','limitations'])assert(s[k]!=null,`${s.id}: missing ${k}`)}
const sourceIds=new Set(sources.map(s=>s.id));for(const i of insights){assert(i.sourceIds.length,`${i.id}: no sources`);assert(i.sourceIds.every(x=>sourceIds.has(x)),`${i.id}: bad source`)}
for(const pdf of pdfManifest){assert(fs.existsSync(pdf.file),`${pdf.id}: supplied PDF missing`);const hash=crypto.createHash('sha256').update(fs.readFileSync(pdf.file)).digest('hex');assert(hash===pdf.sha256,`${pdf.id}: supplied PDF hash changed`);const reader=pdfReader.find(r=>r.sourceId===pdf.id);assert(reader?.pages.length===pdf.pages,`${pdf.id}: reader page count differs`);assert(reader.pages.every((p,i)=>p.page===i+1&&String(p.text).trim()),`${pdf.id}: missing, empty, or misordered reader page`)}
const lessons=curriculum.phases.flatMap(p=>p.levels.flatMap(l=>l.modules));assert(lessons.length>=27,'Too few lessons');assert(ref.workflows.length===15,'Expected 15 workflows');assert(ref.prompts.length===14,'Expected 14 prompts');assert(ref.decisions.length===10,'Expected 10 decisions');assert(ref.checklists.length===15,'Expected 15 checklists');
const lessonIds=new Set(lessons.map(x=>x.id));assert(topics.length>=49,'Topic audit incomplete');for(const t of topics){assert(t.status==='covered',`Uncovered topic ${t.topic}`);assert(lessonIds.has(t.lessonId),`Topic ${t.topic} has invalid lesson`);assert(t.sourceIds.every(x=>sourceIds.has(x)),`Topic ${t.topic} has invalid source`)}
const ontology=JSON.parse(fs.readFileSync(path.join(root,'research','topic-ontology.json'),'utf8'));const ontologyIds=new Set(ontology.topics.map(t=>t.id));
const forensic=JSON.parse(fs.readFileSync(path.join(root,'research','forensic-evidence.json'),'utf8')),expectedEvidence=forensic.sources.flatMap(s=>s.concepts.map(c=>c.id)),mapping=ontology.evidenceRecordMappings,mappedEvidence=new Set([...Object.values(mapping.topicEvidenceRecords).flat(),...Object.keys(mapping.multiTopicAggregateRecords),...Object.keys(mapping.sourceOnlyEvidenceRecords),...Object.keys(mapping.excludedEvidenceRecords)]);assert(expectedEvidence.length===166&&mappedEvidence.size===166&&expectedEvidence.every(id=>mappedEvidence.has(id)),'Forensic evidence ledger is not fully mapped');
assert(topicDetails.length===ontology.topics.length,`Expected ${ontology.topics.length} deep topics, got ${topicDetails.length}`);assert(new Set(topicDetails.map(t=>t.id)).size===topicDetails.length,'Duplicate deep-topic IDs');
const pageCounts=Object.fromEntries(pdfReader.map(s=>[s.sourceId,s.pages.length]));
const words=value=>String(value??'').trim().split(/\s+/).filter(Boolean).length;
const deepText=t=>[t.plainLanguage,t.mentalModel,t.whyItMatters,t.mechanism,...(t.useWhen||[]),...(t.avoidWhen||[]),...(t.procedure||[]),t.workedExample?.scenario,t.workedExample?.input,...(t.workedExample?.actions||[]),t.workedExample?.expectedEvidence,t.badApproach,t.improvedApproach,t.contrastExplanation||t.explanation,...(t.failureModes||[]).flatMap(f=>[f.symptom,f.cause,f.recovery]),t.reusableAid?.content,t.reusableAid?.usage,...(t.checklist||[]),t.exercise,...(Array.isArray(t.successCriteria)?t.successCriteria:[t.successCriteria]),t.advancedNuance].join(' ');
for(const t of topicDetails){
  assert(ontologyIds.has(t.id),`${t.id}: not in canonical ontology`);assert(t.title&&t.phase&&t.evidenceType,`${t.id}: missing identity metadata`);
  assert(words(deepText(t))>=350,`${t.id}: only ${words(deepText(t))} teaching words; minimum is 350`);
  for(const k of ['plainLanguage','mentalModel','whyItMatters','mechanism','badApproach','improvedApproach','exercise','advancedNuance'])assert(words(t[k])>=5,`${t.id}: weak or missing ${k}`);
  assert((t.useWhen||[]).length>=1&&t.avoidWhen?.length>=1,`${t.id}: missing use boundaries`);assert((t.procedure||[]).length>=4,`${t.id}: procedure needs at least four steps`);
  assert(t.workedExample?.scenario&&t.workedExample?.input&&t.workedExample?.actions?.length>=2&&t.workedExample?.expectedEvidence,`${t.id}: incomplete worked example`);
  assert((t.failureModes||[]).length>=2&&t.failureModes.every(f=>f.symptom&&f.cause&&f.recovery),`${t.id}: needs two diagnosis/recovery pairs`);
  assert(t.reusableAid?.type&&words(t.reusableAid?.content)>=5&&words(t.reusableAid?.usage)>=3,`${t.id}: incomplete reusable aid`);assert((t.checklist||[]).length>=3,`${t.id}: checklist too short`);
  assert(t.successCriteria&&(Array.isArray(t.successCriteria)?t.successCriteria.length:words(t.successCriteria)>=5),`${t.id}: missing success criteria`);
  assert(t.citations?.length&&t.citations.every(c=>sourceIds.has(c.sourceId)&&Number.isInteger(c.page)&&c.page>=1&&c.page<=pageCounts[c.sourceId]),`${t.id}: invalid or missing page citation`);
  assert(['used','not_applicable'].includes(t.visualDecision?.status),`${t.id}: visual decision missing`);if(t.visualDecision.status==='used')assert(t.visualDecision.localPath&&fs.existsSync(path.join(root,t.visualDecision.localPath)),`${t.id}: missing visual asset`);else assert(words(t.visualDecision.reason)>=4,`${t.id}: explain why no visual is needed`);
}
assert(topicDetails.every(t=>ontologyIds.has(t.id))&&ontology.topics.every(t=>topicDetails.some(d=>d.id===t.id)),'Deep-topic and ontology IDs differ');
const mappedIds=curriculumMap.phases.flatMap(p=>p.groups.flatMap(g=>g.topicIds));
assert(mappedIds.length===95,`Curriculum map must contain 95 lessons, got ${mappedIds.length}`);assert(new Set(mappedIds).size===95,'Curriculum map contains duplicate lessons');assert(mappedIds.every(id=>ontologyIds.has(id))&&[...ontologyIds].every(id=>mappedIds.includes(id)),'Curriculum map and ontology IDs differ');
const order=Object.fromEntries(mappedIds.map((id,index)=>[id,index]));
for(const [before,after] of [['extensions-taxonomy','extensions-skills'],['extensions-taxonomy','extensions-hooks'],['extensions-taxonomy','extensions-mcp'],['parallel-boundaries','git-worktrees'],['git-worktrees','parallel-background'],['verify-evals','ralph-levels'],['verify-observability','ralph-levels'],['knowledge-corpus-ingest','knowledge-graph-model'],['knowledge-provenance','knowledge-retrieval-cadence']])assert(order[before]<order[after],`${before} must precede ${after}`);
assert(curriculumMap.phases.length===5,'Expected five integrated curriculum phases');assert(!JSON.stringify(curriculumMap).toLowerCase().includes('optional'),'Curriculum map must not segregate optional lessons');
for(const id of ['context-projects-fit','source-cowork-code','source-model-routing','tool-html-infographics','knowledge-retrieval-cadence','source-creative-opinions','source-system-prompt-slimming','source-enterprise-seats','extensions-dx-setup-accounts'])assert(mappedIds.filter(x=>x===id).length===1,`${id} must be integrated exactly once`);
for(const image of images){assert(sourceIds.has(image.sourceId)||image.sourceId==='src-ykdojo-repo',`Image ${image.localPath} has invalid source`);assert(fs.existsSync(path.join(root,image.localPath)),`Missing image ${image.localPath}`)}
const topicMedia=images.filter(i=>String(i.usageLocation).startsWith('topic-'));assert(topicMedia.length>=13,'Meaningful source-media additions are missing');for(const image of topicMedia){assert(topicDetails.some(t=>`topic-${t.id}`===image.usageLocation),`${image.localPath}: invalid topic usage`);assert(Number.isInteger(image.sourcePage)&&image.sourcePage>0,`${image.localPath}: missing source page`);assert(words(image.altText)>=8,`${image.localPath}: alt text is not descriptive`);assert(words(image.caption)>=6,`${image.localPath}: caption is not instructional`);assert(image.attribution,`${image.localPath}: missing attribution`)}
for(const topic of topicDetails.filter(t=>t.visualDecision?.status==='used'))assert(images.some(i=>i.localPath===topic.visualDecision.localPath),`${topic.id}: used visual has no image-manifest record`);
assert(fs.existsSync(path.join(root,'dist','index.html')),'Build dist first');const html=fs.readFileSync(path.join(root,'dist','index.html'),'utf8');assert(!html.includes('__APP_DATA__'),'Data not embedded');
assert(!fs.existsSync(path.join(root,'dist','assets','assets')),'Production build contains duplicate nested media tree');
console.log(`Validated: ${sources.length} sources, ${insights.length} insights, ${lessons.length} lessons, ${topicDetails.length} deep topics, ${ref.workflows.length} workflows, ${ref.prompts.length} prompts.`);
