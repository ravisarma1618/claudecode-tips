(() => {
  'use strict';
  const data=JSON.parse(document.getElementById('app-data').textContent);
  const $=(s,r=document)=>r.querySelector(s), $$=(s,r=document)=>[...r.querySelectorAll(s)];
  const store={get:(k,d)=>{try{return JSON.parse(localStorage.getItem(`ccm:${k}`))??d}catch{return d}},set:(k,v)=>localStorage.setItem(`ccm:${k}`,JSON.stringify(v))};
  const state={complete:new Set(store.get('complete',[])),topicComplete:new Set(store.get('topicComplete',[])),bookmarks:new Set(store.get('bookmarks',[])),checks:store.get('checks',{}),theme:store.get('theme','light')};
  const lessons=data.curriculum.phases.flatMap(p=>p.levels.flatMap(l=>l.modules.map(m=>({...m,phaseId:p.id,phase:p.title,levelId:l.id,levelTitle:l.title}))));
  const insightMap=Object.fromEntries(data.insights.map(i=>[i.id,i]));
  const sourceMap=Object.fromEntries(data.sources.map(s=>[s.id,s]));
  const esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
  const slug=s=>s.toLowerCase().replace(/[^a-z0-9]+/g,'-').replace(/(^-|-$)/g,'');
  const citations=ids=>[...new Set(ids)].map(id=>`<button class="citation" data-source="${id}" title="Open source details">${esc(sourceMap[id]?.title||id)}</button>`).join('');
  const pageCitations=items=>(items||[]).map(c=>`<a class="citation page-citation" href="#reader-${esc(c.sourceId)}-page-${Number(c.page)}" title="Open extracted PDF page">${esc(sourceMap[c.sourceId]?.title||c.sourceId)}, p. ${Number(c.page)}</a>`).join('');
  const codeBlock=(text,label='Copy')=>`<pre><button class="copy" data-copy="${esc(text)}">${label}</button><code>${esc(text)}</code></pre>`;
  const phaseOrder=['orientation','daily-operation','project-systems','advanced-automation','expert-governance'];
  const phaseTitles={orientation:'Phase 1 · Agent foundations', 'daily-operation':'Phase 2 · Daily Claude Code', 'project-systems':'Phase 3 · Reliable project systems', 'advanced-automation':'Phase 4 · Advanced execution', 'expert-governance':'Phase 5 · Expert practice'};
  const domainTitles={'agent-foundations':'Goals, tasks, and agent behavior',prompting:'Give Claude an executable brief',repository:'Understand the repository','context-memory':'Control context and sessions',instructions:'Engineer project instructions',extensions:'Choose reusable extensions','daily-interface':'Operate the terminal interface',tools:'Design tool interfaces',verification:'Verify every change',delivery:'Create checkpoints and deliver',safety:'Establish safety boundaries','multi-agent':'Coordinate parallel work',autonomy:'Engineer bounded autonomy',automation:'Automate repeatable execution','knowledge-systems':'Build trustworthy knowledge systems','adjacent-workflows':'Apply the file workflow beyond code','source-specific':'Dated source perspectives'};
  const domainTitle=d=>domainTitles[d]||String(d||'core-practice').replaceAll('-',' ').replace(/\b\w/g,c=>c.toUpperCase());
  // Learner-facing structure. Domains and difficulty remain filters, never fake "levels".
  const legacyLearningPath=[
    {id:'orientation',title:'Phase 1 · Understand Claude Code',description:'Learn the control loop and the boundaries of agent work.',groups:[
      {id:'control-loop',title:'Control the agent loop',domains:['agent-foundations']},
      {id:'workspace-choice',title:'Choose the right workspace',ids:['source-cowork-code'],optional:true}]},
    {id:'daily-operation',title:'Phase 2 · Work effectively every day',description:'Ask clearly, understand the code, manage context, and verify each change.',groups:[
      {id:'ask-right-work',title:'Ask for the right work',domains:['prompting']},
      {id:'understand-first',title:'Understand before changing',domains:['repository']},
      {id:'context-control',title:'Keep context under control',domains:['context-memory']},
      {id:'daily-interface',title:'Use the interface and terminal well',domains:['daily-interface','tools']},
      {id:'verify-save',title:'Verify and save your work',domains:['verification','delivery']},
      {id:'beyond-code',title:'Use Claude Code beyond programming',domains:['adjacent-workflows']}]},
    {id:'project-systems',title:'Phase 3 · Build reliable project workflows',description:'Turn good individual sessions into a repeatable project system.',groups:[
      {id:'plan-large',title:'Plan larger changes',domains:['agent-foundations','prompting','repository']},
      {id:'project-memory',title:'Preserve project memory',domains:['context-memory']},
      {id:'project-instructions',title:'Write useful project instructions',domains:['instructions']},
      {id:'reusable-capabilities',title:'Add reusable capabilities',domains:['extensions','tools']},
      {id:'test-deliver',title:'Test, review, and deliver',domains:['verification','delivery']},
      {id:'safety-boundaries',title:'Set safety boundaries',domains:['safety']}]},
    {id:'advanced-automation',title:'Phase 4 · Coordinate and automate safely',description:'Scale execution without losing ownership, evidence, or stopping rules.',groups:[
      {id:'bounded-automation',title:'Build bounded automation',domains:['agent-foundations','extensions','safety']},
      {id:'parallel',title:'Coordinate work in parallel',domains:['multi-agent']},
      {id:'ralph',title:'Build a Ralph workflow step by step',domains:['autonomy']},
      {id:'repeatable-commands',title:'Automate repeatable commands',domains:['automation']}]},
    {id:'expert-governance',title:'Phase 5 · Improve and govern your system',description:'Measure outcomes, maintain the system, and evaluate changing advice.',groups:[
      {id:'measure',title:'Measure what actually works',domains:['verification']},
      {id:'operating-system',title:'Maintain your personal operating system',domains:['instructions','tools','automation','adjacent-workflows']},
      {id:'advanced-autonomy',title:'Apply advanced autonomy patterns',domains:['autonomy']},
      {id:'knowledge',title:'Build a knowledge system you can trust',domains:['knowledge-systems']},
      {id:'perspectives',title:'Evaluate changing product advice',domains:['source-specific'],optional:true}]}
  ];
  const learningPath=data.curriculumMap.phases;
  const groupTopics=(topics,phase,group)=>group.topicIds.map(id=>topics.find(t=>t.id===id)).filter(Boolean);
  const orderedTopics=learningPath.flatMap(p=>p.groups.flatMap(g=>groupTopics(data.topicDetails||[],p,g)));
  const courseLocation=Object.fromEntries(learningPath.flatMap(phase=>phase.groups.flatMap(group=>group.topicIds.map(id=>[id,{phase,group,optional:Boolean(phase.optional||group.optional||(group.optionalTopicIds||[]).includes(id))}]))));
  const qualificationMap={
    'context-projects-fit':{badge:'Practitioner pattern',supported:'A supplied practitioner source describes this context-retention pattern.',synthesis:'The lesson converts it into a decision about stable versus exploratory material.',verify:'Confirm current Projects behavior and limits.'},
    'source-cowork-code':{badge:'Verify current product behavior',supported:'A supplied source compares coding and coordinated knowledge-work surfaces.',synthesis:'The durable lesson is matching the working surface to the task.',verify:'Recheck current names, capabilities, availability, and limits.'},
    'source-model-routing':{badge:'Verify current product behavior',supported:'A supplied source describes model and effort routing.',synthesis:'Route from measured uncertainty, impact, latency, and cost.',verify:'Recheck current models, controls, availability, and pricing.'},
    'tool-html-infographics':{badge:'Practitioner pattern',supported:'A supplied source demonstrates HTML as an editable visual artifact.',synthesis:'The lesson focuses on inspectability and version control.',verify:'Treat historical capability comparisons as dated.'},
    'knowledge-retrieval-cadence':{badge:'Discussion-derived insight',supported:'The supplied discussion argues that stored knowledge needs resurfacing.',synthesis:'The lesson adds retrieval, correction, and retirement routines.',verify:'Choose cadence from observed use.'},
    'source-creative-opinions':{badge:'Practitioner viewpoint',supported:'A supplied source offers creative-work and adoption opinions.',synthesis:'Treat each opinion as a bounded hypothesis.',verify:'Measure it in your workflow before retaining it.'},
    'source-system-prompt-slimming':{badge:'Risky workaround',supported:'A supplied source describes system-prompt slimming and patching.',synthesis:'Prefer supported instructions and reversible experiments.',verify:'Isolate and revalidate after every upgrade.'},
    'source-enterprise-seats':{badge:'Verify current policy and contract',supported:'A supplied source makes enterprise, privacy, and commercial claims.',synthesis:'Use a procurement and data-governance verification procedure.',verify:'Confirm current privacy, retention, pricing, and contract terms.'},
    'extensions-dx-setup-accounts':{badge:'Third-party workflow',supported:'The supplied material references a convenience plugin and account workflow.',synthesis:'Evaluate provenance, permissions, maintenance, and fallback.',verify:'Inspect the current package, permissions, maintainer status, and uninstall path.'}
  };
  function renderNav(){
    const topics=data.topicDetails||[];
    $('#courseNav').innerHTML=learningPath.map((p,i)=>`<details ${i===0?'open':''}><summary>${esc(p.title)}</summary>${p.groups.map((g,j)=>`<details class="nav-level" ${i===0&&j===0?'open':''}><summary>${esc(g.title)}</summary>${groupTopics(topics,p,g).map(t=>`<a href="#topic-${t.id}">${esc(t.title)}</a>`).join('')}</details>`).join('')}</details>`).join('');
  }
  const lessonVisuals={
    'lesson-agent-loop':{src:'assets/source-images/src-ykdojo-repo/vibe-coding-spectrum.png',alt:'Spectrum from fast vibe coding to carefully verified engineering',caption:'Match verification rigor to project risk. Source: Y.K. Sugi.'},
    'lesson-prompt-briefs':{src:'assets/source-images/src-ykdojo-repo/voice-transcription-mistakes.png',alt:'Claude Code interpreting imperfect voice-transcribed instructions',caption:'Rich intent can survive imperfect dictation when the goal and constraints are clear. Source: Y.K. Sugi.'},
    'lesson-plan-slices':{src:'assets/source-images/src-ykdojo-repo/breakdown-steps.png',alt:'A large task decomposed into smaller steps from A to B',caption:'Decomposition replaces one brittle leap with verifiable intermediate states. Source: Y.K. Sugi.'},
    'lesson-context':{src:'assets/source-images/src-ykdojo-repo/multitasking-terminal-tabs.png',alt:'Multiple terminal tabs organized as separate Claude Code workstreams',caption:'Separate workstreams keep context coherent; limit the number you actively supervise. Source: Y.K. Sugi.'},
    'lesson-parallel':{src:'assets/source-images/src-ykdojo-repo/git-worktrees.png',alt:'Git worktrees diagram with separate branches in separate directories',caption:'Worktrees isolate concurrent writers at the filesystem and branch levels. Source: Y.K. Sugi.'},
    'lesson-automation':{src:'assets/source-images/src-ykdojo-repo/manual-exponential-backoff.png',alt:'Terminal showing progressively delayed checks of a long-running build',caption:'A bounded polling loop obtains status without flooding context with continuous output. Source: Y.K. Sugi.'},
    'lesson-operating-manual':{src:'assets/source-images/src-ykdojo-repo/writing-assistant-side-by-side.png',alt:'Claude Code terminal beside an editor in a collaborative writing workflow',caption:'A practical operating system pairs conversation with durable files and human review. Source: Y.K. Sugi.'},
    'lesson-agentic-harness':{src:'assets/pdf-images/pdf-agentic-15/page-008-image-02.png',alt:'Diagram of an agent with a persistent goal, tools, and permission to continue',caption:'An agent is organized around a continuing goal, not a single response. PDF: 15 Agentic AI Concepts, p. 8.'},
    'lesson-state-memory':{src:'assets/pdf-images/pdf-agentic-15/page-014-image-02.png',alt:'Diagram separating run-specific context from information saved to memory',caption:'Memory is selected information carried across runs; it is not the entire transcript. PDF: 15 Agentic AI Concepts, p. 14.'},
    'lesson-tool-design':{src:'assets/pdf-images/pdf-agentic-15/page-013-image-02.png',alt:'Diagram showing an agent using tools to read a file and call an API',caption:'Tools give the model bounded actions rather than only opinions. PDF: 15 Agentic AI Concepts, p. 13.'},
    'lesson-orchestration':{src:'assets/pdf-images/pdf-agentic-15/page-019-image-02.png',alt:'Orchestration diagram coordinating a sequence of agent work',caption:'Orchestration is dependency and handoff design, not merely adding agents. PDF: 15 Agentic AI Concepts, p. 19.'},
    'lesson-guardrails-human':{src:'assets/pdf-images/pdf-agentic-15/page-018-image-02.png',alt:'Diagram showing automatic action, pause, human review, and confirmation',caption:'Human review belongs at risk boundaries. PDF: 15 Agentic AI Concepts, p. 18.'},
    'lesson-evals-observability':{src:'assets/pdf-images/pdf-agentic-15/page-021-image-02.png',alt:'Observability diagram exposing an agent execution path instead of a black box',caption:'Tracing makes the execution path inspectable. PDF: 15 Agentic AI Concepts, p. 21.'},
    'lesson-stopping-recovery':{src:'assets/pdf-images/pdf-agentic-15/page-022-image-02.png',alt:'Stopping-condition diagram with explicit success and exit rules',caption:'Every agent needs explicit success, failure, and escalation exits. PDF: 15 Agentic AI Concepts, p. 22.'},
    'lesson-ralph-loop':{src:'assets/pdf-images/pdf-ralph-loop/page-013-image-02.png',alt:'Ralph loop architecture showing prompt, task state, progress, iteration, and completion',caption:'Ralph uses file-based state and repeated verified iterations. PDF: Claude Code Ralph Loop, p. 13.'},
    'lesson-claudemd-layers':{src:'assets/pdf-images/pdf-claudemd-masterclass/page-009-image-02.png',alt:'CLAUDE.md hierarchy diagram showing global, project, and nested instruction scopes',caption:'Instruction hierarchy applies rules at different scopes. PDF: CLAUDE.md Masterclass, p. 9.'},
    'lesson-hooks-subagents':{src:'assets/pdf-images/pdf-claudemd-masterclass/page-033-image-02.png',alt:'Diagram showing CLAUDE.md describing a rule and a hook enforcing it automatically',caption:'Prose describes policy; hooks enforce deterministic policy. PDF: CLAUDE.md Masterclass, p. 33.'},
    'lesson-knowledge-graph':{src:'assets/pdf-images/pdf-graphify-obsidian/page-007-image-01.png',alt:'Graph visualization of connected concepts extracted from a document collection',caption:'A document collection becomes a navigable graph of concepts and relationships. PDF: Graphify + Obsidian + Claude Code, p. 7.'},
    'lesson-daily-interface':{src:'assets/pdf-images/pdf-ykdojo-repo/page-004-image-01.png',alt:'Claude Code tips repository visual demonstrating the daily interface and status workflow',caption:'Daily interface feedback makes branch, session, and context state visible. PDF: ykdojo Claude Code tips, p. 4.'}
  };
  function visualForLesson(id){const v=lessonVisuals[id];return v?`<figure class="lesson-visual"><img src="${v.src}" alt="${esc(v.alt)}" loading="lazy"><figcaption>${esc(v.caption)}</figcaption></figure>`:'';}
  function renderLesson(m,index){
    const insights=m.insightIds.map(id=>insightMap[id]).filter(Boolean); const src=insights.flatMap(i=>i.sourceIds); const advanced=insights.map(i=>`<h4>${esc(i.title)}</h4><p>${esc(i.explanation)}</p><p><strong>Why it works:</strong> ${esc(i.whyItWorks)}</p><p><strong>When not to use it:</strong> ${esc(i.whenNotToUse.join('; '))}</p><p><strong>Failure modes:</strong> ${esc(i.mistakes.join('; '))}</p><p><strong>Advanced variation:</strong> ${esc(i.advancedVariation)}</p>`).join('');
    const options=m.quiz.options.map((o,i)=>`<label><input type="radio" name="quiz-${m.id}" value="${i}"> ${esc(o)}</label>`).join('');
    return `<details class="lesson searchable" id="${m.id}" data-level="${(m.levelId||'level-foundation').replace('level-','')}" data-topic="${m.topics.join(' ')}" data-source="${src.join(' ')}" data-difficulty="${m.difficulty}" data-type="${m.type}" open>
      <summary><div class="lesson-title"><span class="meta">${esc(m.levelTitle)} · Difficulty ${m.difficulty} · ${esc(m.type)}</span><h3>${esc(m.title)}</h3><span class="meta">${esc(m.objective)}</span></div><div class="lesson-actions"><button class="bookmark ${state.bookmarks.has(m.id)?'active':''}" data-bookmark="${m.id}" aria-label="Bookmark ${esc(m.title)}" title="Bookmark">★</button></div></summary>
      <div class="lesson-body"><p><span class="label">Source-supported + synthesis</span></p><p class="concept">${esc(m.simple)}</p>${visualForLesson(m.id)}<h4>Repeatable procedure</h4><ol class="steps">${m.procedure.map(x=>`<li>${esc(x)}</li>`).join('')}</ol>
      <div class="tabs" data-tabs><div class="tab-list" role="tablist"><button role="tab" aria-selected="true" data-tab="example-${m.id}">Improved</button><button role="tab" aria-selected="false" data-tab="mistake-${m.id}">Mistake</button></div><div id="example-${m.id}" class="tab-panel good" role="tabpanel">${codeBlock(m.good)}</div><div id="mistake-${m.id}" class="tab-panel bad" role="tabpanel" hidden>${esc(m.bad)}</div></div>
      <details class="advanced" open><summary>Advanced nuance and evidence</summary>${advanced}<div class="citations"><strong>Sources:</strong> ${citations(src)}</div></details>
      <div class="exercise"><h4>Practice</h4><p>${esc(m.exercise)}</p><p><strong>Success criterion:</strong> ${esc(m.success)}</p></div>
      <div class="quiz"><h4>Knowledge check</h4><p>${esc(m.quiz.question)}</p><div class="quiz-options">${options}</div><button data-quiz="${m.id}" data-answer="${m.quiz.answer}" data-explanation="${esc(m.quiz.explanation)}">Check answer</button><p class="quiz-result" aria-live="polite"></p></div>
      <div class="lesson-footer"><button class="complete-toggle ${state.complete.has(m.id)?'done':''}" data-complete="${m.id}">${state.complete.has(m.id)?'✓ Completed':'Mark complete'}</button><span class="meta">Lesson ${index+1} of ${lessons.length}</span></div>
      <nav class="lesson-pager" aria-label="Lesson navigation">${index?`<a href="#${lessons[index-1].id}">← ${esc(lessons[index-1].title)}</a>`:'<span></span>'}${index<lessons.length-1?`<a href="#${lessons[index+1].id}">${esc(lessons[index+1].title)} →</a>`:''}</nav></div></details>`;
  }
  function renderCurriculum(){let n=0;$('#curriculum').innerHTML=data.curriculum.phases.map(p=>`<details class="phase" id="${p.id}" open><summary><h2>${esc(p.title)}</h2><span class="phase-progress" data-phase-progress="${p.id}"></span><p>${esc(p.description)}</p></summary>${p.levels.map(l=>`<details class="level" id="${l.id}" open><summary>${esc(l.title)}</summary>${l.modules.map(m=>renderLesson({...m,phaseId:p.id,phase:p.title,levelId:l.id,levelTitle:l.title},n++)).join('')}</details>`).join('')}</details>`).join('');}
  const topicDetailMap=Object.fromEntries((data.topicDetails||[]).map(t=>[t.id,t]));
  const sourceMechanismMap=Object.fromEntries((data.sourceMechanisms||[]).map(x=>[x.topicId,x]));
  function sourceMechanisms(t){const bundle=sourceMechanismMap[t.id];if(!bundle?.records?.length)return'';return `<section class="source-mechanisms"><h4>What the supplied sources specifically teach</h4><p class="source-nuance">${esc(bundle.nuance)}</p>${bundle.records.map(r=>`<article><div><strong>${esc(r.title)}</strong><span class="label">${esc(r.confidence)} confidence</span></div><p>${esc(r.evidence)}</p>${r.details?.length?`<ul>${r.details.map(x=>`<li>${esc(x)}</li>`).join('')}</ul>`:''}${r.warnings?.length?`<div class="source-warning"><strong>Source warning</strong><ul>${r.warnings.map(x=>`<li>${esc(x)}</li>`).join('')}</ul></div>`:''}<footer>${pageCitations((r.pages||[]).map(page=>({sourceId:r.sourceId,page})))}</footer></article>`).join('')}</section>`;}
  function topicVisual(t){
    const mapped=data.images.find(i=>i.usageLocation===`topic-${t.id}`),fallback=t.visualDecision?.status==='used'&&t.visualDecision?.localPath?t.visualDecision:null;
    if(!mapped&&!fallback)return `<p class="visual-note"><strong>Visual decision:</strong> ${esc(t.visualDecision?.reason||'No instructional visual is necessary for this concept.')}</p>`;
    const record=mapped||data.images.find(i=>i.localPath===fallback.localPath),src=mapped?.localPath||fallback.localPath,alt=mapped?.altText||fallback.altText||t.title,caption=mapped?.caption||fallback.caption||'Source visual supporting this concept.',page=mapped?.sourcePage||Number(src.match(/page-(\d+)/)?.[1]),sourceId=mapped?.sourceId||record?.sourceId,attribution=mapped?.attribution||record?.attribution||sourceMap[sourceId]?.title;
    return `<figure class="lesson-visual"><img src="${esc(src)}" alt="${esc(alt)}" loading="lazy"><figcaption>${esc(caption)}${attribution?` <span class="visual-attribution">Source: ${esc(attribution)}${page&&!String(attribution).includes(`p. ${page}`)?`, p. ${page}`:''}.</span>`:''}</figcaption></figure>`;
  }
  function renderTopicBody(t){
    const failures=(t.failureModes||[]).map(f=>`<li><strong>${esc(f.symptom)}</strong><span><b>Cause:</b> ${esc(f.cause)}</span><span><b>Recovery:</b> ${esc(f.recovery)}</span></li>`).join('');
    const actions=t.workedExample?.actions||[],aid=t.reusableAid||{},qualification=qualificationMap[t.id];
    const instructionalVisual=topicVisual(t);
    const evidenceBoundary=qualification?`<aside class="evidence-boundary" aria-label="Evidence boundary: ${esc(qualification.badge)}"><h4>${esc(qualification.badge)}</h4><p><strong>Supported by supplied source:</strong> ${esc(qualification.supported)}</p><p><strong>Instructor synthesis:</strong> ${esc(qualification.synthesis)}</p><p><strong>Verify now:</strong> ${esc(qualification.verify)}</p></aside>`:'';
    const visual=evidenceBoundary+instructionalVisual+sourceMechanisms(t);
    const all=orderedTopics,position=all.findIndex(x=>x.id===t.id),prereqs=(t.prerequisites||[]).map(id=>topicDetailMap[id]?`<a href="#topic-${esc(id)}">${esc(topicDetailMap[id].title)}</a>`:esc(id)).join(', ')||'None';
    return `<article class="topic-dossier-body"><div class="topic-lead"><span class="label">${esc((t.evidenceType||'source-supported').replaceAll('_',' '))}</span><p>${esc(t.plainLanguage)}</p><h4>Mental model</h4><p>${esc(t.mentalModel)}</p><p class="meta"><strong>Prerequisites:</strong> ${prereqs}</p></div>${visual}<div class="teaching-grid"><section><h4>Why this matters</h4><p>${esc(t.whyItMatters)}</p></section><section><h4>How it works</h4><p>${esc(t.mechanism)}</p></section></div><div class="use-boundaries"><section><h4>Use it when</h4><ul>${(t.useWhen||[]).map(x=>`<li>${esc(x)}</li>`).join('')}</ul></section><section><h4>Avoid or adapt it when</h4><ul>${(t.avoidWhen||[]).map(x=>`<li>${esc(x)}</li>`).join('')}</ul></section></div><h4>Repeatable procedure</h4><ol class="steps">${(t.procedure||[]).map(x=>`<li>${esc(x)}</li>`).join('')}</ol><section class="worked-example"><h4>Worked application</h4><p><strong>Scenario:</strong> ${esc(t.workedExample?.scenario)}</p><p><strong>Starting input:</strong> ${esc(t.workedExample?.input)}</p><ol>${actions.map(x=>`<li>${esc(x)}</li>`).join('')}</ol><p><strong>Expected evidence:</strong> ${esc(t.workedExample?.expectedEvidence)}</p></section><div class="contrast-grid"><section class="bad"><h4>Weak approach</h4><p>${esc(t.badApproach)}</p></section><section class="good"><h4>Improved approach</h4><p>${esc(t.improvedApproach)}</p></section></div><p><strong>Why the improved version works:</strong> ${esc(t.contrastExplanation||t.explanation)}</p><h4>Failure diagnosis and recovery</h4><ul class="failure-list">${failures}</ul><section class="reusable-aid"><h4>Reusable ${esc(aid.type||'aid')}</h4>${codeBlock(aid.content||'')}<p>${esc(aid.usage)}</p></section><section class="topic-checklist"><h4>Checklist</h4>${(t.checklist||[]).map((x,i)=>`<label class="check-item"><input type="checkbox" data-check="topic-${t.id}:${i}" ${state.checks[`topic-${t.id}:${i}`]?'checked':''}> <span>${esc(x)}</span></label>`).join('')}</section><details class="advanced" open><summary>Advanced nuance</summary><p>${esc(t.advancedNuance)}</p></details><section class="exercise"><h4>Practice</h4><p>${esc(t.exercise)}</p><p><strong>Mastery evidence:</strong> ${esc(Array.isArray(t.successCriteria)?t.successCriteria.join('; '):t.successCriteria)}</p></section><footer class="topic-evidence"><strong>PDF evidence:</strong> ${pageCitations(t.citations)}<button class="complete-toggle topic-complete ${state.topicComplete.has(t.id)?'done':''}" data-topic-complete="${t.id}">${state.topicComplete.has(t.id)?'✓ Lesson completed':'Mark lesson complete'}</button></footer><nav class="lesson-pager" aria-label="Curriculum lesson navigation">${position>0?`<a href="#topic-${all[position-1].id}">← ${esc(all[position-1].title)}</a>`:'<span></span>'}<span class="meta">Lesson ${position+1} of ${all.length}</span>${position<all.length-1?`<a href="#topic-${all[position+1].id}">${esc(all[position+1].title)} →</a>`:''}</nav></article>`;
  }
  function hydrateTopic(el){if(!el||el.dataset.hydrated)return;const t=topicDetailMap[el.dataset.topicId];if(t){el.insertAdjacentHTML('beforeend',renderTopicBody(t));el.dataset.hydrated='true';}}
  const pdfReaderMap=Object.fromEntries((data.pdfReader||[]).map(r=>[r.sourceId,r]));
  function hydratePdfPage(el){if(!el||el.dataset.hydrated)return;const page=pdfReaderMap[el.dataset.pdfSource]?.pages?.find(p=>p.page===Number(el.dataset.page));if(page){el.insertAdjacentHTML('beforeend',`<pre class="page-text">${esc(page.text)}</pre>`);el.dataset.hydrated='true';}}
  function renderTopicHandbook(){
    const topics=data.topicDetails||[];
    $('#topicHandbook').innerHTML=phaseOrder.filter(phase=>topics.some(t=>t.phase===phase)).map(phase=>{const phaseTopics=topics.filter(t=>t.phase===phase),domains=[...new Set(phaseTopics.map(t=>t.domain))];return `<details class="topic-phase" id="handbook-${slug(phase)}" open><summary><span><strong>${esc(phaseTitles[phase])}</strong><small><span data-topic-phase-progress="${phase}"></span> · ${phaseTopics.length} lessons</small></span></summary><div class="topic-phase-body">${domains.map((domain,levelIndex)=>{const levelTopics=phaseTopics.filter(t=>t.domain===domain);return `<details class="topic-level" id="level-${slug(phase+'-'+domain)}" open><summary><span>Level ${levelIndex+1} · ${esc(domainTitle(domain))}</span><small>${levelTopics.length} lessons</small></summary><div class="topic-level-body">${levelTopics.map((t,index)=>`<details class="topic-dossier searchable" id="topic-${t.id}" data-topic-id="${t.id}" data-level="${esc(t.difficulty<=2?'foundation':t.difficulty<=3?'productive':t.difficulty===4?'advanced':'expert')}" data-topic="${esc(t.id+' '+domain+' '+(t.tags||[]).join(' '))}" data-source="${esc((t.citations||[]).map(c=>c.sourceId).join(' '))}" data-difficulty="${Number(t.difficulty)||1}" data-type="lesson"><summary><span class="topic-number">${String(index+1).padStart(2,'0')}</span><span><strong>${esc(t.title)}</strong><small>${esc((t.plainLanguage||t.mentalModel).split(/(?<=[.!?])\s/)[0])}</small></span><span class="depth-badge">${state.topicComplete.has(t.id)?'Completed':'Not started'}</span></summary></details>`).join('')}</div></details>`}).join('')}</div></details>`}).join('');
  }
  function renderSimpleTopicHandbook(){
    const topics=data.topicDetails||[];
    let lessonNumber=0;
    $('#topicHandbook').innerHTML=learningPath.map((phase,phaseIndex)=>{
      const phaseTopics=phase.groups.flatMap(group=>groupTopics(topics,phase,group));
      return `<details class="topic-phase" id="handbook-${phase.id}" ${phaseIndex===0?'open':''}><summary><span><strong>${esc(phase.title)}</strong><small><span data-topic-phase-progress="${phase.id}"></span> · ${phaseTopics.length} lessons</small></span><p>${esc(phase.description)}</p></summary><div class="topic-phase-body">${phase.groups.map((group,groupIndex)=>{
        const groupLessons=groupTopics(topics,phase,group);
        return `<details class="topic-level" id="group-${group.id}" ${phaseIndex===0&&groupIndex===0?'open':''}><summary><span>${esc(group.title)}${group.optional?' · Optional':''}</span><small>${groupLessons.length} ${groupLessons.length===1?'lesson':'lessons'}</small></summary><div class="topic-level-body">${groupLessons.map(t=>{
          lessonNumber++;
          const optional=Boolean(phase.optional||group.optional||(group.optionalTopicIds||[]).includes(t.id));
          return `<details class="topic-dossier searchable ${optional?'optional-lesson':''}" id="topic-${t.id}" data-topic-id="${t.id}" data-level="${esc(t.difficulty<=2?'foundation':t.difficulty<=3?'productive':t.difficulty===4?'advanced':'expert')}" data-topic="${esc(t.id+' '+group.title+' '+(t.tags||[]).join(' '))}" data-source="${esc((t.citations||[]).map(c=>c.sourceId).join(' '))}" data-difficulty="${Number(t.difficulty)||1}" data-type="lesson"><summary><span class="topic-number">${String(lessonNumber).padStart(2,'0')}</span><span><strong>${esc(t.title)}</strong><small>${esc((t.plainLanguage||t.mentalModel).split(/(?<=[.!?])\s/)[0])}</small></span><span class="depth-badge">${state.topicComplete.has(t.id)?'Completed':optional?'Optional':'Not started'}</span></summary></details>`;
        }).join('')}</div></details>`;
      }).join('')}</div></details>`;
    }).join('');
  }
  function renderPremiumPractice(){
    const practice=data.premiumPractice;
    $('#practiceGrid').innerHTML=practice.projects.map(p=>`<details class="practice-project searchable" id="${p.id}" data-level="advanced" data-topic="professional-project ${p.primaryTopicIds.join(' ')}" data-source="${p.evidenceSourceIds.join(' ')}" data-difficulty="4" data-type="project"><summary><span><strong>${p.order}. ${esc(p.title)}</strong><small>${esc(p.estimatedHours)} hours · ${p.guidedLabs.length} guided labs · ${p.primaryTopicIds.length} linked lessons</small></span></summary><div class="practice-body"><p class="practice-scenario"><strong>Professional scenario:</strong> ${esc(p.professionalScenario)}</p><p><strong>Objective:</strong> ${esc(p.objective)}</p><h3>Required deliverables</h3><ul>${p.deliverables.map(x=>`<li>${esc(x)}</li>`).join('')}</ul><h3>Guided laboratories</h3>${p.guidedLabs.map(l=>`<details class="guided-lab"><summary>${esc(l.title)}</summary><ol>${l.steps.map(x=>`<li>${esc(x)}</li>`).join('')}</ol><p><strong>Proof:</strong> ${esc(l.proof)}</p></details>`).join('')}<section class="independent-assignment"><h3>Independent assignment</h3><p>${esc(p.independentAssignment.brief)}</p><h4>Constraints</h4><ul>${p.independentAssignment.constraints.map(x=>`<li>${esc(x)}</li>`).join('')}</ul><p><strong>Submit:</strong> ${esc(p.independentAssignment.submission)}</p></section><h3>100-point grading rubric</h3><div class="rubric-wrap"><table class="rubric-table"><thead><tr><th>Criterion</th><th>Weight</th><th>Exemplary</th><th>Proficient</th><th>Needs work</th></tr></thead><tbody>${p.rubric.map(r=>`<tr><th>${esc(r.criterion)}</th><td>${r.weight}</td><td>${esc(r.exemplary)}</td><td>${esc(r.proficient)}</td><td>${esc(r.needsWork)}</td></tr>`).join('')}</tbody></table></div><details class="model-solution"><summary>Reveal model-solution outline</summary><ol>${p.modelSolutionOutline.map(x=>`<li>${esc(x)}</li>`).join('')}</ol></details><h3>Professional checklist</h3>${p.professionalChecklist.map((x,i)=>`<label class="check-item"><input type="checkbox" data-check="premium-${p.id}:${i}" ${state.checks[`premium-${p.id}:${i}`]?'checked':''}> <span>${esc(x)}</span></label>`).join('')}<p class="mastery-gate"><strong>Mastery gate:</strong> ${esc(p.masteryGate)}</p><nav class="linked-topics" aria-label="Lessons supporting ${esc(p.title)}"><strong>Supporting lessons:</strong> ${p.primaryTopicIds.map(id=>`<a href="#topic-${id}">${esc(topicDetailMap[id]?.title||id)}</a>`).join('')}</nav><footer><strong>Evidence sources:</strong> ${citations(p.evidenceSourceIds)}</footer></div></details>`).join('');
    $('#capstoneTrack').innerHTML=`<section class="capstone-track"><h3>Capstone progression</h3><ol>${practice.capstoneProgression.map(s=>`<li><a href="#${s.projectId}"><strong>Stage ${s.stage}: ${esc(s.professionalArtifact)}</strong></a><span>${esc(s.independence)} · ${esc(s.gate)}</span></li>`).join('')}</ol></section><section class="professional-checklists"><h3>Cross-project operating checks</h3>${practice.professionalChecklists.map(c=>`<details><summary>${esc(c.title)}</summary>${c.items.map((x,i)=>`<label class="check-item"><input type="checkbox" data-check="premium-global-${c.id}:${i}" ${state.checks[`premium-global-${c.id}:${i}`]?'checked':''}> <span>${esc(x)}</span></label>`).join('')}</details>`).join('')}</section>`;
  }
  function renderReferences(){
    renderSimpleTopicHandbook();
    renderPremiumPractice();
    $('#workflowGrid').innerHTML=data.reference.workflows.map(w=>`<article class="card workflow-card searchable" id="${w.id}" data-level="productive" data-topic="workflow ${slug(w.title)}" data-source="${w.sourceIds.join(' ')}" data-difficulty="2" data-type="workflow"><span class="label">Workflow</span><h3>${esc(w.title)}</h3><p>${esc(w.objective)}</p><details><summary>Open procedure</summary><h4>Preconditions</h4><ul>${w.preconditions.map(x=>`<li>${esc(x)}</li>`).join('')}</ul><h4>Procedure</h4><ol class="steps">${w.steps.map(x=>`<li>${esc(x)}</li>`).join('')}</ol><h4>Recommended prompt</h4>${codeBlock(w.prompt)}<h4>Verification</h4><ul>${w.checks.map(x=>`<li>${esc(x)}</li>`).join('')}</ul><h4>Failure modes</h4><p>${esc(w.failures.join('; '))}</p><h4>Recovery</h4><p>${esc(w.recovery)}</p><p>${citations(w.sourceIds)}</p></details></article>`).join('');
    $('#promptGrid').innerHTML=data.reference.prompts.map(p=>`<article class="card searchable" id="${p.id}" data-level="productive" data-topic="prompting ${slug(p.category)}" data-source="" data-difficulty="2" data-type="prompt"><span class="label">${esc(p.category)}</span><h3>${esc(p.title)}</h3>${codeBlock(p.text)}<p class="meta">${esc(p.guidance)}</p></article>`).join('');
    $('#decisionGrid').innerHTML=data.reference.decisions.map(d=>`<article class="card searchable" id="${d.id}" data-level="advanced" data-topic="decision" data-source="" data-difficulty="3" data-type="decision"><span class="label">Decision tree</span><h3>${esc(d.question)}</h3><div class="decision-buttons"><button data-decision="${d.id}" data-choice="0">Yes / likely</button><button data-decision="${d.id}" data-choice="1">No / unsure</button></div><div class="decision-result" aria-live="polite">Choose a direction to reveal the governing rule.</div><script type="application/json" class="decision-data">${JSON.stringify(d.rules).replaceAll('<','\\u003c')}</script></article>`).join('');
    $('#antiGrid').innerHTML=data.reference.antiPatterns.map((a,i)=>`<article class="card searchable" id="anti-${i}" data-level="productive" data-topic="anti-pattern" data-source="${a.sourceIds.join(' ')}" data-difficulty="2" data-type="anti-pattern"><span class="label">Anti-pattern</span><h3>${esc(a.title)}</h3><p><strong>Symptoms:</strong> ${esc(a.symptoms)}</p><details><summary>Diagnosis and correction</summary><p><strong>Root cause:</strong> ${esc(a.root)}</p><p><strong>Why it fails:</strong> ${esc(a.why)}</p><p><strong>Correction:</strong> ${esc(a.correction)}</p><p><strong>Prevention:</strong> ${esc(a.prevention)}</p>${citations(a.sourceIds)}</details></article>`).join('');
    $('#checklistGrid').innerHTML=data.reference.checklists.map(c=>`<article class="card searchable" id="check-${c.id}" data-level="productive" data-topic="checklist" data-source="" data-difficulty="2" data-type="checklist"><span class="label">Checklist</span><h3>${esc(c.title)}</h3>${c.items.map((x,i)=>`<label class="check-item"><input type="checkbox" data-check="${c.id}:${i}" ${state.checks[`${c.id}:${i}`]?'checked':''}> <span>${esc(x)}</span></label>`).join('')}</article>`).join('');
    $('#glossaryGrid').innerHTML=data.reference.glossary.map(g=>`<dt id="term-${slug(g.term)}">${esc(g.term)}</dt><dd>${esc(g.definition)}</dd>`).join('');
    const audited=data.topicDetails||[];$('#topicCoverage').innerHTML=`<div class="coverage-summary"><strong>${audited.length}</strong><span>canonical topics</span><strong>${audited.filter(t=>String(t.evidenceType).includes('source-specific')||String(t.evidenceType).includes('source_only')||String(t.evidenceType).includes('source-only')).length}</strong><span>qualified source claims</span><strong>${data.sources.length}</strong><span>supplied PDFs</span></div><div class="coverage-table-wrap"><table class="coverage-table"><thead><tr><th>Topic</th><th>Deep teaching</th><th>Page-level PDF evidence</th><th>Status</th></tr></thead><tbody>${audited.map(t=>`<tr><td>${esc(t.title)}</td><td><a href="#topic-${t.id}">Open complete dossier</a></td><td>${pageCitations(t.citations)}</td><td><span class="status complete">depth validated</span></td></tr>`).join('')}</tbody></table></div>`;
    $('#pdfReader').innerHTML=data.pdfReader.map(r=>{const source=sourceMap[r.sourceId];const visuals=data.images.filter(i=>i.sourceId===r.sourceId&&i.usageLocation!=='supporting comparison asset');return `<details class="pdf-volume searchable" id="reader-${r.sourceId}" data-level="" data-topic="pdf-reader" data-source="${r.sourceId}" data-difficulty="" data-type="source-detail"><summary><span><strong>${esc(r.title)}</strong><small>${r.pages.length} pages · ${r.concepts.length} indexed headings/steps · ${source?.wordCount||0} words</small></span></summary><div class="pdf-volume-body"><h3>Detected headings, tips, and numbered steps</h3>${r.concepts.length?`<nav class="concept-index" aria-label="Detected page index in ${esc(r.title)}">${r.concepts.map(c=>`<a href="#reader-${r.sourceId}-page-${c.page}">${esc(c.title)}</a>`).join('')}</nav>`:'<p class="meta">This PDF uses prose or screenshots rather than consistently extractable headings. Every page remains available below.</p>'}${visuals.length?`<h3>Instructional screenshots and diagrams</h3><div class="pdf-gallery">${visuals.map(v=>`<figure><img src="${esc(v.localPath)}" alt="${esc(v.altText)}" loading="lazy"><figcaption>${esc(v.caption)} · ${esc(v.attribution)}</figcaption></figure>`).join('')}</div>`:''}<h3>Complete page-by-page extraction</h3><p class="reader-note">Text is preserved from the locally supplied PDF and may contain line-wrap or encoding artifacts. Open the original PDF when exact visual layout matters.</p><div class="pdf-pages">${r.pages.map(p=>`<details class="pdf-page" id="reader-${r.sourceId}-page-${p.page}" data-pdf-source="${r.sourceId}" data-page="${p.page}"><summary>Page ${p.page}</summary></details>`).join('')}</div></div></details>`}).join('');
    $('#sourceGrid').innerHTML=data.sources.map(s=>`<article class="source-card searchable" id="source-${s.id}" data-level="" data-topic="source" data-source="${s.id}" data-difficulty="" data-type="source"><span class="status ${s.status}">${s.status.replace('_',' ')}</span><h3>${esc(s.title)}</h3><p class="meta">${esc(s.author||'Author unavailable')} · ${esc(s.publishedDate||'Date unavailable')}</p><p>${esc(s.coverageNotes)}</p><button data-source="${s.id}">View extraction record</button> <a href="${esc(s.url)}" target="_blank" rel="noopener">Open original ↗</a></article>`).join('');
  }
  function updateProgress(){
    const done=orderedTopics.filter(t=>state.topicComplete.has(t.id)).length;const pct=orderedTopics.length?Math.round(done/orderedTopics.length*100):0;$('#progressLabel').textContent=`${pct}% complete`;$('#progressBar').value=pct;$('#progressBar').textContent=`${pct}%`;
    learningPath.forEach(phase=>{const phaseTopics=phase.groups.flatMap(group=>groupTopics(data.topicDetails||[],phase,group)),done=phaseTopics.filter(t=>state.topicComplete.has(t.id)).length;const el=$(`[data-topic-phase-progress="${phase.id}"]`);if(el)el.textContent=`${done}/${phaseTopics.length} ${phase.optional?'optional':'complete'}`;});
    data.curriculum.phases.forEach(p=>{const mods=p.levels.flatMap(l=>l.modules);const done=mods.filter(m=>state.complete.has(m.id)).length;$(`[data-phase-progress="${p.id}"]`).textContent=`${done}/${mods.length} complete`;});
  }
  function initFilters(){
    const topics=[...new Set($$('.searchable').flatMap(e=>e.dataset.topic.split(' ')).filter(Boolean))].sort();$('#topicFilter').insertAdjacentHTML('beforeend',topics.map(t=>`<option value="${t}">${t.replaceAll('-',' ')}</option>`).join(''));
    $('#sourceFilter').insertAdjacentHTML('beforeend',data.sources.map(s=>`<option value="${s.id}">${esc(s.title)}</option>`).join(''));
    const types=[...new Set($$('.searchable').map(e=>e.dataset.type).filter(Boolean))].sort();$('#typeFilter').insertAdjacentHTML('beforeend',types.map(t=>`<option value="${t}">${t}</option>`).join(''));
  }
  function clearMarks(){ $$('mark').forEach(m=>m.replaceWith(document.createTextNode(m.textContent))); }
  function applyFilters(){
    clearMarks();const q=$('#searchInput').value.trim().toLowerCase();const vals={level:$('#levelFilter').value,topic:$('#topicFilter').value,source:$('#sourceFilter').value,difficulty:$('#difficultyFilter').value,type:$('#typeFilter').value};let count=0;
    $$('.searchable').forEach(el=>{const text=(el.dataset.topicId?JSON.stringify(topicDetailMap[el.dataset.topicId]):el.classList.contains('pdf-volume')?JSON.stringify(pdfReaderMap[el.dataset.source]):el.textContent).toLowerCase();const ok=(!q||text.includes(q))&&Object.entries(vals).every(([k,v])=>!v||(el.dataset[k]||'').split(' ').includes(v));el.classList.toggle('hidden-by-filter',!ok);if(ok){count++;if(q){const walker=document.createTreeWalker(el,NodeFilter.SHOW_TEXT,{acceptNode:n=>n.parentElement.closest('button,script,style,mark')?NodeFilter.FILTER_REJECT:n.nodeValue.toLowerCase().includes(q)?NodeFilter.FILTER_ACCEPT:NodeFilter.FILTER_REJECT});const nodes=[];while(walker.nextNode())nodes.push(walker.currentNode);nodes.slice(0,3).forEach(n=>{const i=n.nodeValue.toLowerCase().indexOf(q),mark=document.createElement('mark');mark.textContent=n.nodeValue.slice(i,i+q.length);n.replaceWith(document.createTextNode(n.nodeValue.slice(0,i)),mark,document.createTextNode(n.nodeValue.slice(i+q.length)));});}}});
    $('#emptyState').hidden=count>0;$('#searchStatus').textContent=`${count} matching sections`;$$('.phase,.level').forEach(d=>{const visible=$$('.searchable',d).some(e=>!e.classList.contains('hidden-by-filter'));d.classList.toggle('hidden-by-filter',!visible);if(q&&visible)d.open=true;});
  }
  function showSource(id){const s=sourceMap[id];if(!s)return;$('#sourceDialogBody').innerHTML=`<span class="status ${s.status}">${esc(s.status)}</span><h2 id="sourceDialogTitle">${esc(s.title)}</h2><p><strong>Author:</strong> ${esc(s.author||'Unavailable')}<br><strong>Published:</strong> ${esc(s.publishedDate||'Unavailable')}<br><strong>Retrieved:</strong> ${esc(s.retrievedAt||'Unavailable')}<br><strong>Method:</strong> ${esc(s.extractionMethod)}</p><p><strong>Coverage:</strong> ${esc(s.coverageNotes)}</p><h3>Limitations</h3><ul>${s.limitations.map(x=>`<li>${esc(x)}</li>`).join('')}</ul><p><strong>Extraction indicators:</strong> ${s.pages||'—'} pages · ${s.headingsFound} headings · ${s.imagesFound} candidate images · ${s.wordCount} body words</p><p><strong>Local PDF:</strong> <code>${esc(s.localRawFile||'Unavailable')}</code><br><strong>SHA-256:</strong> <code class="checksum">${esc(s.sha256||'Unavailable')}</code></p><p><a href="${esc(s.url)}" target="_blank" rel="noopener">Open canonical source ↗</a></p>`;$('#sourceDialog').showModal();}
  function toast(msg){const t=$('#toast');t.textContent=msg;t.classList.add('show');setTimeout(()=>t.classList.remove('show'),1600);}
  function events(){
    document.addEventListener('toggle',e=>{if(!e.target.open)return;if(e.target.matches?.('.topic-dossier'))hydrateTopic(e.target);if(e.target.matches?.('.pdf-page'))hydratePdfPage(e.target);},true);
    document.addEventListener('click',e=>{const b=e.target.closest('button');if(!b)return;
      if(b.dataset.copy!=null){navigator.clipboard?.writeText(b.dataset.copy).then(()=>toast('Copied to clipboard')).catch(()=>{const ta=document.createElement('textarea');ta.value=b.dataset.copy;document.body.append(ta);ta.select();document.execCommand('copy');ta.remove();toast('Copied to clipboard');});}
      if(b.dataset.bookmark){const id=b.dataset.bookmark;state.bookmarks.has(id)?state.bookmarks.delete(id):state.bookmarks.add(id);store.set('bookmarks',[...state.bookmarks]);b.classList.toggle('active',state.bookmarks.has(id));toast(state.bookmarks.has(id)?'Bookmarked':'Bookmark removed');}
      if(b.dataset.complete){const id=b.dataset.complete;state.complete.has(id)?state.complete.delete(id):state.complete.add(id);store.set('complete',[...state.complete]);b.classList.toggle('done',state.complete.has(id));b.textContent=state.complete.has(id)?'✓ Completed':'Mark complete';updateProgress();}
      if(b.dataset.topicComplete){const id=b.dataset.topicComplete;state.topicComplete.has(id)?state.topicComplete.delete(id):state.topicComplete.add(id);store.set('topicComplete',[...state.topicComplete]);b.classList.toggle('done',state.topicComplete.has(id));b.textContent=state.topicComplete.has(id)?'✓ Lesson completed':'Mark lesson complete';const dossier=b.closest('.topic-dossier');const badge=dossier?.querySelector('.depth-badge');if(badge)badge.textContent=state.topicComplete.has(id)?'Completed':'Depth validated';updateProgress();}
      if(b.dataset.source)showSource(b.dataset.source);
      if(b.dataset.quiz){const selected=$(`input[name="quiz-${b.dataset.quiz}"]:checked`);const out=b.parentElement.querySelector('.quiz-result');out.textContent=!selected?'Choose an answer first.':Number(selected.value)===Number(b.dataset.answer)?`Correct. ${b.dataset.explanation}`:`Not yet. ${b.dataset.explanation}`;}
      if(b.dataset.tab){const root=b.closest('[data-tabs]');$$('[role=tab]',root).forEach(x=>x.setAttribute('aria-selected',x===b));$$('[role=tabpanel]',root).forEach(x=>x.hidden=x.id!==b.dataset.tab);}
      if(b.dataset.decision){const root=b.closest('.card'),rules=JSON.parse($('.decision-data',root).textContent);$('.decision-result',root).textContent=rules[Number(b.dataset.choice)];}
    });
    document.addEventListener('change',e=>{if(e.target.dataset.check){state.checks[e.target.dataset.check]=e.target.checked;store.set('checks',state.checks);}if(e.target.matches('#levelFilter,#topicFilter,#sourceFilter,#difficultyFilter,#typeFilter'))applyFilters();});
    $('#searchInput').addEventListener('input',applyFilters);$('#clearFilters').onclick=()=>{$('#searchInput').value='';$$('.filters select').forEach(s=>s.value='');applyFilters();};
    $('#expandAll').onclick=()=>$$('details').forEach(d=>d.open=true);$('#collapseAll').onclick=()=>$$('details').forEach(d=>d.open=false);
    $('#advancedToggle').onchange=e=>$$('.advanced').forEach(d=>d.hidden=!e.target.checked);
    $('#themeToggle').onclick=()=>{state.theme=document.documentElement.dataset.theme==='dark'?'light':'dark';document.documentElement.dataset.theme=state.theme;store.set('theme',state.theme);};
    $('#printButton').onclick=()=>window.print();$('#backToTop').onclick=()=>scrollTo({top:0,behavior:'smooth'});addEventListener('scroll',()=>$('#backToTop').classList.toggle('visible',scrollY>500),{passive:true});
    const drawer=v=>{$('#sidebar').classList.toggle('open',v);$('#drawerScrim').hidden=!v;$('#menuToggle').setAttribute('aria-expanded',String(v));};$('#menuToggle').onclick=()=>drawer(true);$('#menuClose').onclick=()=>drawer(false);$('#drawerScrim').onclick=()=>drawer(false);$('#courseNav').addEventListener('click',e=>{if(e.target.closest('a'))drawer(false)});
    $('.dialog-close').onclick=()=>$('#sourceDialog').close();$('#sourceDialog').addEventListener('click',e=>{if(e.target===$('#sourceDialog'))$('#sourceDialog').close();});
    addEventListener('hashchange',()=>{updateCrumbs();restoreInitialHash()});updateCrumbs();
  }
  function updateCrumbs(){const id=location.hash.slice(1),topic=id.startsWith('topic-')?topicDetailMap[id.slice(6)]:null,locationInfo=topic?courseLocation[topic.id]:null,m=lessons.find(x=>x.id===id);$('#crumbs').textContent=topic?`Curriculum / ${locationInfo.phase.title} / ${locationInfo.group.title} / ${topic.title}`:m?`Quick start / ${m.phase} / ${m.title}`:id?`Reference / ${id.replaceAll('-',' ')}`:'Home';}
  function restoreInitialHash(){const id=decodeURIComponent(location.hash.slice(1));if(!id)return;const target=document.getElementById(id);if(!target)return;if(target.matches('.topic-dossier'))hydrateTopic(target);if(target.matches('.pdf-page'))hydratePdfPage(target);target.matches('details')&&(target.open=true);let parent=target.parentElement?.closest('details');while(parent){parent.open=true;parent=parent.parentElement?.closest('details')}const align=()=>target.scrollIntoView({block:'start'});const images=$$('img');images.forEach(img=>img.loading='eager');requestAnimationFrame(align);Promise.allSettled(images.map(img=>img.decode?.()||Promise.resolve())).then(align);const observer=new ResizeObserver(align);observer.observe($('#main'));setTimeout(()=>{observer.disconnect();align();},5000);}
  function init(){document.documentElement.dataset.theme=state.theme;renderNav();renderCurriculum();renderReferences();initFilters();updateProgress();$('#metrics').innerHTML=[[data.sources.length,'PDFs processed'],[data.insights.length,'atomic insights'],[(data.topicDetails||[]).length,'curriculum lessons'],[lessons.length,'quick starts']].map(x=>`<div class="metric"><strong>${x[0]}</strong><span>${x[1]}</span></div>`).join('');events();restoreInitialHash();}
  init();
})();
