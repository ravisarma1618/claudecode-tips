import fs from 'node:fs';
import path from 'node:path';
const root=path.resolve(import.meta.dirname,'..'),topicFile=path.join(root,'data','topic-details.json');
const topics=JSON.parse(fs.readFileSync(topicFile,'utf8')),ontology=JSON.parse(fs.readFileSync(path.join(root,'research','topic-ontology.json'),'utf8')).topics,mechanisms=JSON.parse(fs.readFileSync(path.join(root,'data','source-mechanisms.json'),'utf8')),premium=new Set(JSON.parse(fs.readFileSync(path.join(root,'data','premium-topic-overrides-teacher.json'),'utf8')).topics.map(x=>x.id)),images=JSON.parse(fs.readFileSync(path.join(root,'data','image-manifest.json'),'utf8'));
const byOntology=Object.fromEntries(ontology.map(x=>[x.id,x])),byMechanism=Object.fromEntries(mechanisms.map(x=>[x.topicId,x]));
const profiles={
  'agent-foundations':{role:'platform lead',repo:'Atlas orchestration service',change:'stabilizing an agent that processes support incidents',artifact:'runbook/agent-control.md',command:'npm test -- agent-control',output:'a transition trace with continue, stop, and escalation reasons'},
  prompting:{role:'product engineer',repo:'Northstar onboarding application',change:'implementing an accessible team-invitation flow',artifact:'specs/team-invitation.md',command:'npm test -- invitation',output:'a reviewed brief, scoped diff, and passing invitation tests'},
  repository:{role:'staff engineer',repo:'Harbor monorepo',change:'adding CSV export without breaking billing imports',artifact:'docs/repository-map.md',command:'rg --files; rg "billing|export" apps packages tests',output:'a file-and-dependency map followed by a focused test report'},
  'context-memory':{role:'incident engineer',repo:'Beacon payments API',change:'handing a failed idempotency fix to a clean session',artifact:'handoffs/payment-idempotency.md',command:'git diff --stat; npm test -- idempotency',output:'a compact handoff whose claims reproduce from files and command logs'},
  instructions:{role:'monorepo maintainer',repo:'Mercury commerce workspace',change:'standardizing database migrations across three packages',artifact:'CLAUDE.md and packages/database/CLAUDE.md',command:'git diff -- CLAUDE.md packages/database/CLAUDE.md',output:'a clean-session instruction-loading check and scoped diff'},
  extensions:{role:'developer-platform engineer',repo:'Kepler internal tooling repository',change:'packaging a repeatable release investigation capability',artifact:'.claude/settings.json and .claude/skills/release-investigation/SKILL.md',command:'claude /release-investigation --dry-run',output:'a capability-selection record, permission trace, and deterministic dry-run result'},
  'daily-interface':{role:'application engineer',repo:'Lighthouse web application',change:'running two visible workstreams without losing session state',artifact:'terminal session labels and statusline configuration',command:'git status --short; pwd',output:'correct branch, path, context, and task labels in each terminal'},
  tools:{role:'tooling engineer',repo:'Relay automation gateway',change:'adding a bounded issue-search tool',artifact:'schemas/issue-search.schema.json',command:'npm test -- issue-search',output:'validated JSON, explicit exit codes, timeout behavior, and stderr diagnostics'},
  verification:{role:'quality engineer',repo:'Orion subscription service',change:'fixing a renewal bug with independent behavioral proof',artifact:'tests/renewal-regression.test.ts',command:'npm test -- renewal-regression; git diff --check',output:'a red-then-green test record, clean diff check, and reviewer findings'},
  delivery:{role:'release engineer',repo:'Summit API',change:'preparing a rate-limit change for pull-request review',artifact:'branch feat/rate-limit-policy and PR evidence packet',command:'git status; git diff main...HEAD; gh pr checks',output:'a focused commit, passing checks, review disposition, and rollback note'},
  safety:{role:'security engineer',repo:'Vault administration service',change:'automating a read-heavy audit while protecting production credentials',artifact:'.claude/settings.json permission policy',command:'claude --permission-mode plan; git diff -- .claude/settings.json',output:'allowed reads, denied destructive action, approval event, and immutable audit log'},
  'multi-agent':{role:'technical lead',repo:'Constellation monorepo',change:'parallelizing API, UI, and test work with one integrator',artifact:'plans/ownership-matrix.md and isolated worktrees',command:'git worktree list; git status --short',output:'non-overlapping ownership, explicit handoffs, isolated branches, and an integration report'},
  autonomy:{role:'automation lead',repo:'Foundry SDK',change:'qualifying a Ralph loop for a bounded compatibility migration',artifact:'prd.json, progress.txt, and agents.md',command:'npm test; npm run lint; git status --short',output:'iteration logs, task-state transitions, verifier exit codes, and a controller stop reason'},
  automation:{role:'operations engineer',repo:'Pulse maintenance toolkit',change:'batching independent dependency checks with bounded retries',artifact:'jobs/dependency-audit.json',command:'claude -p "audit dependencies" --output-format json',output:'machine-readable stdout, per-job exit status, retry count, and final aggregate report'},
  'knowledge-systems':{role:'knowledge architect',repo:'Research vault staging workspace',change:'importing architecture evidence without losing provenance',artifact:'vault-staging/notes/*.md and import-manifest.json',command:'rg "source_file|source_page" vault-staging',output:'reversible notes, typed links, source backlinks, checksums, and an archive decision'},
  'adjacent-workflows':{role:'technical writer',repo:'Product decision handbook',change:'turning interview material into a versioned operating guide',artifact:'handbook/decision-guide.md',command:'git diff --word-diff handbook/decision-guide.md',output:'a reviewable prose diff, source links, rejected style patterns, and approval record'},
  'source-specific':{role:'technology strategy lead',repo:'Current-tooling evaluation workspace',change:'testing a dated practitioner claim before organizational adoption',artifact:'evaluations/claim-ledger.md',command:'git diff -- evaluations/claim-ledger.md',output:'dated source claim, current verification result, bounded experiment, and keep-or-reject decision'}
};
const firstWord=s=>String(s).match(/[A-Za-z0-9]+/)?.[0]||'Lesson';
const prose=v=>Array.isArray(v)?v.map(prose).join(' '):v&&typeof v==='object'?Object.entries(v).map(([k,x])=>`${k}: ${prose(x)}`).join(' '):String(v??'');
for(const [index,topic] of topics.entries()){
  const o=byOntology[topic.id],bundle=byMechanism[topic.id],record=bundle.records[0],p=profiles[o.domain]||profiles.prompting,details=(record.details||[]),warnings=(record.warnings||[]),sourceRule=record.evidence,keyword=firstWord(topic.title);
  if(!premium.has(topic.id)){
    topic.plainLanguage=`${topic.title} is the discipline of applying this source rule in a real engineering system: ${sourceRule} In ${p.repo}, it controls a named decision rather than serving as a slogan.`;
    topic.mentalModel=`Picture ${topic.title.toLowerCase()} as the control point between ${p.artifact} and ${p.output}. The source's decisive distinction is: ${o.nuance}`;
    topic.whyItMatters=`Without this control, the ${p.role} can receive a plausible narrative while repository state remains wrong. The resulting cost—an unreproducible decision, unsafe transition, or ownerless regression—follows directly from this source observation: ${details[0]||sourceRule}`;
    topic.mechanism=`The mechanism operates on ${p.artifact}. First it exposes the relevant state; then it applies ${o.nuance} Finally, ${p.command} produces ${p.output}. ${details[1]||warnings[0]||`${record.recordId} applies this source observation before the next action: ${sourceRule}`}`;
    topic.useWhen=[`Use ${topic.title.toLowerCase()} while ${p.change}.`,`Use it when the source condition is present: ${details[0]||sourceRule}`];
    topic.avoidWhen=[`Do not add ${topic.title.toLowerCase()} when ${p.output} is already produced by a smaller deterministic check.`,warnings[0]||`For ${record.recordId}, avoid extra coordination when this source condition is absent: ${sourceRule}`];
    topic.procedure=[`Open ${p.artifact} and record the current state relevant to ${topic.title.toLowerCase()}.`,`Read the cited source rule—${record.title}—and translate it into one decision for ${p.change}.`,`Run ${p.command} before editing; preserve its status, exit code, or diff as the baseline.`,`Make the smallest change that exercises ${o.nuance}`,`Run ${p.command} again and compare the observed output with the baseline and acceptance rule.`,`Record the decision, failed boundary case, owner, and recovery path in ${p.artifact}.`];
    topic.workedExample={scenario:`${p.role[0].toUpperCase()+p.role.slice(1)} Maya owns ${p.repo} and is ${p.change}. A prior Claude session ignored this concrete source observation—${sourceRule}—so the ${record.recordId} review must be reproducible without the chat.`,input:`The branch contains a partial implementation, ${p.artifact}, and the last saved command is ${p.command}. The ${record.recordId} boundary from ${sourceRule} is deliberately violated so diagnosis must expose the mechanism.`,actions:[`Maya captures a ${record.recordId} baseline and identifies the state changed by ${details[0]||sourceRule}.`,`Before editing, Claude cites relevant files and predicts the ${record.recordId} observable defined by ${sourceRule}.`,`Claude makes one bounded change, runs ${p.command}, and stores output needed to evaluate ${details[1]||o.nuance}.`,`Maya injects a counterexample derived from ${warnings[0]||sourceRule}; the ${record.recordId} failure is classified and repaired at the responsible layer.`,`A clean reviewer receives ${p.artifact}, the diff, and the ${record.recordId} evidence packet, then independently accepts or rejects the claim.`],expectedEvidence:`The ${record.title} submission contains ${p.output}; it names changed state, preserves the command or inspection path, demonstrates one failed source boundary and recovery, and lets a reviewer reproduce the conclusion without trusting the original conversation.`};
    topic.badApproach=`Ask Claude to “apply ${topic.title} and make it production ready,” accept a polished explanation, and omit ${p.artifact}, ${p.command}, and the boundary condition from review.`;
    topic.improvedApproach=`For ${p.change}, inspect ${p.artifact}, apply this constraint—${o.nuance}—make one reversible change, run ${p.command}, and stop unless ${p.output} directly supports the acceptance claim.`;
    topic.contrastExplanation=`The weak ${record.recordId} version names ${record.title} but supplies no operational state. The improved version binds ${topic.title.toLowerCase()} to ${p.artifact}, an action boundary, ${p.command}, and evidence capable of disproving the claim.`;
    topic.failureModes=[{symptom:`${keyword} output looks complete but ${p.output} is absent.`,cause:`${topic.title} was treated as explanatory prose instead of a control over ${p.artifact}.`,recovery:`Reopen ${p.artifact}, rerun ${p.command}, and reject completion until the missing evidence is attached.`},{symptom:`${keyword} ${record.recordId} execution passes the happy path but fails its source boundary.`,cause:`The ${record.recordId} workflow omitted this distinction: ${o.nuance}`,recovery:`Add a ${record.recordId} counterexample, repair the responsible layer, and preserve its failing and passing outputs.`},{symptom:`${keyword} ${record.recordId} evidence cannot be reproduced in a clean session.`,cause:`Required state remained in chat instead of ${p.artifact}.`,recovery:`Write the decision and exact command to ${p.artifact}, start a clean review session, and repeat the verification.`}];
    topic.reusableAid={type:'professional evidence brief',content:`${topic.title}\nRepository: ${p.repo}\nChange: ${p.change}\nArtifact: ${p.artifact}\nVerification: ${p.command}\nRequired output: ${p.output}\nSource distinction: ${o.nuance}\nStop: preserve evidence and escalate when the boundary cannot be reproduced.`,usage:`Complete this brief before implementation and attach it to the independent review packet for ${p.repo}.`};
    topic.checklist=[`${p.artifact} records the state controlled by ${topic.title}.`,`${p.command} has a saved baseline and final result.`,`A counterexample exercises ${o.nuance}`,`The reviewer can reproduce ${p.output} without the original chat.`];
    topic.exercise=`In a disposable branch of ${p.repo}, reproduce the scenario for ${p.change}. Deliberately violate the source distinction, capture the failed ${p.command} result, repair the responsible layer, and submit ${p.artifact}, the diff, both outputs, and a 150-word defense of the stop decision.`;
    topic.successCriteria=[`${topic.title} is connected to ${p.artifact} and a named owner.`,`${p.command} produces a saved pass or fail output and exit status.`,`The ${record.recordId} counterexample derived from ${record.title} is observable in the report.`,`A clean reviewer reproduces ${p.output} from the submitted files.`];
    topic.advancedNuance=`At professional scale, measure ${topic.title.toLowerCase()} through ${p.output}. The ${record.recordId} evaluation tracks false completion, recovery time, and reviewer disagreement against ${sourceRule} The cited source supplies the mechanism; the ${p.repo} rubric is instructor-authored.`;
  } else {
    if(!Array.isArray(topic.workedExample?.actions)){const original=topic.workedExample||{};topic.workedExample={scenario:topic.repositoryScenario,input:prose(original.input||original.weak||topic.repositoryScenario),actions:Object.entries(original).filter(([k])=>!['input','expectedEvidence','evidence'].includes(k)).map(([k,v])=>`${k}: ${prose(v)}`),expectedEvidence:prose(original.expectedEvidence||original.evidence||`${p.output} recorded in ${p.artifact}`)};}
    topic.workedExample.scenario||=topic.repositoryScenario;topic.workedExample.input||=topic.repositoryScenario;topic.workedExample.expectedEvidence||=prose(topic.workedExample.evidence||`${p.output} recorded in ${p.artifact}`);
    topic.mentalModel=`Treat ${topic.title.toLowerCase()} as a repository protocol: ${o.nuance} The protocol is valid only when its files and verifier outputs agree.`;
    topic.whyItMatters=topic.contrastExplanation;
    topic.useWhen=[`Use it for ${topic.repositoryScenario}`,`Use it when ${record.evidence}`];
    topic.avoidWhen=[warnings[0]||`Avoid this mechanism when a single deterministic command solves the task with lower coordination cost.`,`Do not promote autonomy until the submitted mastery rubric passes in a clean review.`];
    topic.reusableAid={type:'implementation record',content:`${topic.title}\nRepository artifact: ${p.artifact}\nVerification command: ${p.command}\nRequired evidence: ${p.output}\nSource distinction: ${o.nuance}`,usage:'Attach this record to the lesson exercise and reviewer handoff.'};
    topic.checklist=topic.successCriteria.map(x=>`${topic.title}: ${x}`);
    topic.advancedNuance=`Promotion beyond the worked scenario requires representative evaluation, explicit cost and iteration ceilings, rollback, and a reviewer who did not supervise the original run. ${record.evidence}`;
    topic.workedExample=JSON.parse(JSON.stringify(topic.workedExample).replaceAll('[path]','packages/billing'));
    topic.workedExample.input=`${topic.workedExample.input} Repository context: ${topic.repositoryScenario}`;
    topic.workedExample.actions.push(`The reviewer inspects ${p.artifact}, runs ${p.command}, and records whether ${record.title} produced ${p.output}.`);
    topic.procedure=[`Inspect ${p.artifact} and save the baseline from ${p.command}.`,...topic.procedure];
    topic.successCriteria=topic.successCriteria.map((x,i)=>i<2?`${topic.title}: ${x} is visible in a file, command output, status, or trace.`:`${topic.title}: ${x}`);
  }
  topic.failureModes=topic.failureModes.map(f=>({...f,symptom:`${topic.title}: ${String(f.symptom).replace(new RegExp(`^${keyword}:?\\s*`,'i'),'')}`}));
  topic.citations=(topic.citations||[]).map(c=>({...c,support:`The cited page supports the ${topic.title.toLowerCase()} mechanism, constraint, warning, or artifact used in this professional application.`}));
  topic.evidenceType='source-supported';topic.evidenceNote='Cited PDF pages support the named mechanism. Repository scenarios, exercises, recovery demonstrations, and grading criteria are instructor-authored professional applications.';
  const media=images.find(i=>i.usageLocation===`topic-${topic.id}`)||images.find(i=>i.localPath===topic.visualDecision?.localPath);
  if(media)topic.visualDecision={status:'used',localPath:media.localPath,sourceId:media.sourceId,page:media.sourcePage||Number(media.localPath.match(/page-(\d+)/)?.[1])||topic.citations.find(c=>c.sourceId===media.sourceId)?.page,attribution:media.attribution,altText:(media.altText.split(/\s+/).length>=8?media.altText:`Source diagram illustrating ${topic.title} through related states, actions, and evidence`),caption:(media.caption.split(/\s+/).length>=8?media.caption:`This source visual shows the operational relationship taught by ${topic.title}.`)};
  else topic.visualDecision={status:'not_applicable',reason:`The cited source explains ${record.title} as text and operational evidence; mastery of ${topic.title} is judged from ${p.artifact} and ${p.output}, so an additional screenshot would not clarify a spatial or sequential relationship.`};
}
fs.writeFileSync(topicFile,JSON.stringify(topics,null,2)+'\n');
console.log(`Rewrote ${topics.length-premium.size} dossiers from source mechanisms and retained ${premium.size} teacher-authored premium replacements.`);
